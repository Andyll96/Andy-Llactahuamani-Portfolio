//Gets all of the images in the page and stores them in an HTML collection
//console.log(document.images);

//gets elements by Tag name and stores them in an array
let imgs = document.getElementsByTagName('img');
console.log(imgs);